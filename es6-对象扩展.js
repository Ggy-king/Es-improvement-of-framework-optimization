// Object.is
/**
 * 判断两个值是否相等 注意是===
 * Object.is(120,120) // true
 */


Object.assign
/**
 * 对象的合并 浅拷贝语法糖 后面的覆盖前面
 * Object.assign(config1,config2)   （config1和config2是两个对象）
 */

Object.setPrototypeOf
/**
 * 设置原型对象
 * Object.setPrototypeOf(one,two) 给one设置原型对象two
 * 即给该实例对象构造函数设置原型对象
 */
Object.getPrototypeOf
/**
 * Object.getPrototypeOf(one) 获取one身上的原型对象
 */